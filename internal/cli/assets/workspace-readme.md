# loop workspace

This directory was scaffolded by `loop init`.

- `pipelines/` — deterministic stage workflows. Run with `loop run <file>`.
- `rooms/` — multi-agent chat rooms. Open with `loop chat <file>`.

Start here:

    loop validate pipelines/feature-delivery.yaml
    loop validate rooms/leadership.yaml
